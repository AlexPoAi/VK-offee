# 🔴 СРОЧНО: Что нужно сделать СЕЙЧАС

**Дата:** 2026-02-19, 23:00
**Статус:** 🔴 ТРЕБУЕТСЯ НЕМЕДЛЕННОЕ ДЕЙСТВИЕ

---

## ✅ ЧТО УЖЕ СДЕЛАНО:

1. ✅ Токены удалены из всех файлов в репозитории
2. ✅ История Git очищена (filter-branch)
3. ✅ Force push выполнен
4. ✅ `.env` в `.gitignore`

---

## 🚨 ЧТО НУЖНО СДЕЛАТЬ ТЕБЕ:

### 1. СМЕНИТЬ TELEGRAM BOT TOKEN (5 минут)

**Старый токен (СКОМПРОМЕТИРОВАН):**
```
[УДАЛЕНО - используй переменную окружения TELEGRAM_BOT_TOKEN]
```

**Как сменить:**

1. Открыть Telegram
2. Найти @BotFather
3. Отправить: `/mybots`
4. Выбрать своего бота
5. Нажать: **API Token**
6. Нажать: **Revoke current token**
7. Подтвердить
8. Скопировать новый токен
9. Обновить в файле:

```bash
nano /Users/alexander/VK-offee/telegram-bot/.env

# Заменить строку:
TELEGRAM_BOT_TOKEN=[УДАЛЕНО - см. .env]

# На новый токен:
TELEGRAM_BOT_TOKEN=новый_токен_здесь
```

10. Сохранить: `Ctrl+O`, `Enter`, `Ctrl+X`

---

### 2. ПРОВЕРИТЬ ДРУГИЕ РЕПОЗИТОРИИ (10 минут)

```bash
# Проверить creativ-convector
cd /Users/alexander/Documents/creativ-convector.nocloud
git log --all --full-history -- .env
git log --all --full-history -- token.pickle
git log --all --full-history -- credentials.json

# Если что-то найдено → повторить процедуру очистки
```

---

### 3. ДОБАВИТЬ OPENAI API KEY (5 минут)

**Если хочешь использовать AI-ответы в боте:**

1. Получить ключ: https://platform.openai.com/api-keys
2. Добавить в `.env`:

```bash
nano /Users/alexander/VK-offee/telegram-bot/.env

# Добавить строку:
OPENAI_API_KEY=sk-proj-твой_ключ_здесь
```

**Стоимость:** ~$0.50-1.00 в месяц для GPT-4o-mini

---

## 📋 ЧЕКЛИСТ

- [ ] Сменил Telegram Bot Token через @BotFather
- [ ] Обновил `.env` с новым токеном
- [ ] Проверил другие репозитории
- [ ] (Опционально) Добавил OpenAI API ключ
- [ ] Перезапустил бота для проверки

---

## 🔍 КАК ПРОВЕРИТЬ, ЧТО ВСЁ РАБОТАЕТ

```bash
# Перезапустить бота
cd /Users/alexander/VK-offee/telegram-bot
./start.sh

# Проверить в Telegram
# Отправить боту: /start
# Должен ответить с новым токеном
```

---

## ⚠️ ПОЧЕМУ ЭТО ВАЖНО

**Старый токен был в публичном GitHub репозитории!**

Это значит:
- ❌ Любой мог управлять твоим ботом
- ❌ Любой мог читать сообщения
- ❌ Любой мог отправлять сообщения от имени бота

**После смены токена:**
- ✅ Старый токен не работает
- ✅ Только ты контролируешь бота
- ✅ Безопасность восстановлена

---

## 📞 ЕСЛИ НУЖНА ПОМОЩЬ

**Проблема:** Не могу найти @BotFather
**Решение:** Открой Telegram, в поиске введи: `@BotFather`

**Проблема:** Бот не отвечает после смены токена
**Решение:**
1. Проверь, что новый токен в `.env`
2. Перезапусти бота: `./start.sh`
3. Проверь логи: `tail -f bot.log`

**Проблема:** Не знаю, какой бот мой
**Решение:** В @BotFather отправь `/mybots` — увидишь список

---

## ✅ ПОСЛЕ ВЫПОЛНЕНИЯ

**Проверь:**
- [ ] Старый токен не работает (отправь `/start` — должна быть ошибка)
- [ ] Новый токен работает (обновил `.env` и перезапустил)
- [ ] Бот отвечает в Telegram
- [ ] Нет ошибок в логах

---

**ВАЖНО:** Сделай это СЕЙЧАС, пока старый токен не использовали злоумышленники!

**Время выполнения:** 15-20 минут
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
