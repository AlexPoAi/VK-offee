# История разработки ИИ-агента для сети кофеен

## Дата: 13 февраля 2026

---

## Контекст проекта

**Роль агента:**
- Операционный менеджер
- Менеджер организационного развития
- Кризис-менеджер

**Цель:**
Рост прибыли, управляемости и устойчивости сети кофеен (3 точки)

**Система учёта:** Saby Presto

**Задача:**
Создать ИИ-агента на базе ChatGPT API для анализа работы предприятия

---

## Архитектура решения

### Схема работы:
```
Saby Presto → Excel → Google Drive → Python агент → ChatGPT API
```

**Почему не через GitHub:**
- GitHub не предназначен для постоянно меняющихся данных
- Прямое чтение с Google Drive проще и быстрее
- Нет конфликтов версий

---

## Выполненные шаги

### 1. Настройка Google Drive API

#### Созданные ресурсы:
- **Проект:** coffee-agent-487319
- **Service Account:** coffee-agent@coffee-agent-487319.iam.gserviceaccount.com
- **JSON-ключ:** скачан (нужно сохранить в `/config/google_credentials.json`)

#### Структура папок на Google Drive:
```
/Данные_Саби/
  /продажи/
  /остатки/
  /персонал/
```

**Доступ:**
Папка расшарена на Service Account email

---

### 2. Изучение API Saby Presto

#### Проблема:
Публичный API Saby Presto **НЕ предоставляет** методы для получения отчётов по продажам/выручке.

#### Что есть в API:
- Получить остатки товаров
- Получить каталог/прайс-лист
- Создать заказы на доставку
- Бронирование столиков

#### Чего НЕТ:
- Данные по сменам
- Выручка по дням/точкам
- Списания
- Средний чек
- Продажи по позициям

**Документация:** https://saby.ru/help/integration/api/app_presto

---

### 3. Выбранное решение

**Подход:** Ручная выгрузка + автоматизация чтения

**Процесс:**
1. Ежедневно выгружать отчёты из Saby Presto в Excel
2. Сохранять в папку Google Drive Desktop
3. Автоматическая синхронизация в облако
4. Python-агент читает данные с Google Drive
5. Анализ через ChatGPT API

**Затраты времени:** 2 минуты/день на ручную выгрузку

---

## Техническая архитектура агента

### Структура проекта:
```
coffee_agent/
├── config/
│   └── google_credentials.json      # ключ от Google Drive
├── data/
│   └── cache/                        # кэш скачанных файлов
├── src/
│   ├── data_reader.py               # чтение из Google Drive
│   ├── agent.py                     # главный агент
│   ├── tools.py                     # инструменты для агента
│   └── prompts.py                   # промпты
├── main.py                           # точка входа
├── requirements.txt
└── .env                              # ключи API
```

### Зависимости (requirements.txt):
```
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.116.0
pandas==2.2.0
openpyxl==3.1.2
openai==1.12.0
langchain==0.1.6
langchain-openai==0.0.5
python-dotenv==1.0.1
```

### Переменные окружения (.env):
```
OPENAI_API_KEY=твой_ключ_chatgpt
GOOGLE_DRIVE_FOLDER_ID=id_папки_Данные_Саби
```

---

## Структура данных для выгрузки

### Файл 1: продажи_YYYY-MM-DD.xlsx
Обновление: ежедневно

| Дата | Точка | Выручка | Чеков | Средний_чек | Топ_позиция | Топ_продаж |
|------|-------|---------|-------|-------------|-------------|------------|

### Файл 2: остатки_YYYY-MM-DD.xlsx
Обновление: ежедневно

| Дата | Точка | Товар | Остаток | Списание | Причина |
|------|-------|-------|---------|----------|---------|

### Файл 3: персонал_W##.xlsx
Обновление: еженедельно

| Неделя | Точка | Сотрудник | Смен | Выручка_на_смену | Средний_чек |
|--------|-------|-----------|------|------------------|-------------|

---

## Код агента (основные компоненты)

### data_reader.py (чтение с Google Drive):
```python
from google.oauth2 import service_account
from googleapiclient.discovery import build
import pandas as pd
import io

class DriveReader:
    def __init__(self, credentials_path, folder_id):
        self.creds = service_account.Credentials.from_service_account_file(
            credentials_path,
            scopes=['https://www.googleapis.com/auth/drive.readonly']
        )
        self.service = build('drive', 'v3', credentials=self.creds)
        self.folder_id = folder_id
    
    def get_latest_file(self, file_prefix):
        """Найти последний файл по префиксу"""
        query = f"'{self.folder_id}' in parents and name contains '{file_prefix}' and trashed=false"
        results = self.service.files().list(
            q=query,
            orderBy='modifiedTime desc',
            pageSize=1,
            fields='files(id, name, modifiedTime)'
        ).execute()
        
        files = results.get('files', [])
        return files[0] if files else None
    
    def read_excel(self, file_id):
        """Скачать Excel и превратить в DataFrame"""
        request = self.service.files().get_media(fileId=file_id)
        file_content = io.BytesIO(request.execute())
        return pd.read_excel(file_content)
```

### prompts.py (системный промпт):
```python
SYSTEM_PROMPT = """Ты операционный менеджер сети из 3 кофеен.

Роли:
- Анализ выручки и отклонений
- Контроль остатков и списаний
- Оценка работы персонала

Данные доступны через инструменты.

При ответе:
1. Факты (цифры из данных)
2. Анализ (почему так)
3. Решение (3 конкретных действия)
4. Риски
5. Первый шаг (что делать завтра)

Без воды. Структурно.
"""
```

---

## Следующие шаги

### Этап 1: Первая выгрузка из Saby (текущий статус)
**Задача:** Найти как выгружать отчёты из личного кабинета Saby Presto

**Проблема:** Кнопка "Выгрузить" не найдена в интерфейсе

**Варианты решения:**
1. Сделать скриншот интерфейса раздела "Ресторан"
2. Попробовать копирование данных вручную (Ctrl+A → Ctrl+C → Excel)
3. Позвонить в поддержку Saby для уточнения

**Инструкция (из документации):**
- Раздел: rest.saby.ru → "Ресторан"
- Выбрать точку продаж
- Указать период (день, месяц, квартал)
- Данные показываются на экране

---

### Этап 2: Разработка MVP агента
1. Настроить чтение файлов с Google Drive
2. Написать базовые инструменты (tools.py)
3. Интегрировать ChatGPT API
4. Тестирование на реальных данных

### Этап 3: Автоматизация
1. Ежедневный запуск скрипта по расписанию
2. Автоматические алерты (падение выручки >20%, списания >10%)
3. Telegram-бот для запросов

---

## Важные ссылки

### Документация Saby:
- API Presto: https://saby.ru/help/integration/api/app_presto
- Статистика продаж: https://saby.ru/help/presto/kontrol/pay
- Остатки товаров: https://saby.ru/help/integration/api/app_sale/sale_delyvery/leftover_goods

### Поддержка:
- Saby: https://saby.ru/support

---

## Риски и решения

| Риск | Вероятность | Решение |
|------|-------------|---------|
| Saby не выгружает стабильно | Высокая | Ручной бэкап раз в неделю |
| Excel с битыми данными | Средняя | Валидация при чтении |
| ChatGPT галлюцинирует | Средняя | Всегда показывать источник |
| Google Drive API лимиты | Низкая | Кэширование |
| Персонал игнорирует рекомендации | Высокая | Постепенное внедрение |

---

## Принципы работы агента

**Фокус:**
1. Операционный блок: выручка, ФК, средний чек, графики, списания
2. Оргразвитие: регламенты, роли, KPI, обучение
3. Кризис: падение выручки, конфликты, кассовый разрыв

**Мышление:**
- Думай как собственник сети
- Оцени влияние на прибыль
- Конкретные управленческие действия

**Ответы всегда структурно:**
1. Диагностика
2. 3 конкретных решения
3. Риски
4. Пошаговые действия

---

## Заметки

- Все делается на русском языке (названия папок, переменных, файлов)
- Service Account настроен и готов
- Google Drive структура создана
- Ожидается первая выгрузка данных из Saby

**Текущий статус:** Ожидание инструкций по выгрузке из Saby Presto

---

## Контакты для следующего агента

**Данные проекта:**
- Service Account Email: coffee-agent@coffee-agent-487319.iam.gserviceaccount.com
- Папка на Google Drive: Данные_Саби
- Количество точек: 3
- Система учёта: Saby Presto

**Техническое окружение:**
- Python 3.x
- OpenAI API (ChatGPT)
- Google Drive API
- Pandas для обработки Excel

**Следующий шаг:**
Определить способ выгрузки отчётов из Saby Presto и начать разработку MVP агента.
